# ---------------------------------------------------------------------------
# Database (PostgreSQL)
# ---------------------------------------------------------------------------
# Connection string used by the app, drizzle-kit, migrations and the seed.
DATABASE_URL=postgresql://horizon:horizon@localhost:5432/datak_horizon

# Values consumed by docker-compose to provision the local database.
POSTGRES_USER=horizon
POSTGRES_PASSWORD=horizon
POSTGRES_DB=datak_horizon
POSTGRES_PORT=5432

# ---------------------------------------------------------------------------
# Firebase Authentication — client (public, exposed to the browser)
# ---------------------------------------------------------------------------
NUXT_PUBLIC_FIREBASE_API_KEY=
NUXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NUXT_PUBLIC_FIREBASE_PROJECT_ID=
NUXT_PUBLIC_FIREBASE_APP_ID=

# ---------------------------------------------------------------------------
# Firebase Admin SDK & Authorization — server only
# ---------------------------------------------------------------------------
# Path to the service account JSON for local development.
# In GCP, Application Default Credentials will be used automatically.
GOOGLE_APPLICATION_CREDENTIALS=credentials/serviceAccount.json

# Optional comma-separated list of authorized user emails (appended to the
# default versioned allowlist in server/utils/allowlist.ts).
# NUXT_AUTH_ALLOWLIST=user1@datak.co,user2@datak.co

# ---------------------------------------------------------------------------
# Slack Webhook Integration
# ---------------------------------------------------------------------------
# Slack Webhook URL for notifications
NUXT_SLACK_WEBHOOK_URL=
# Base URL of the application to build links in notifications
NUXT_SITE_URL=http://localhost:3000

# ---------------------------------------------------------------------------
# Confluence Integration
# ---------------------------------------------------------------------------
# Domain of Confluence (e.g. your-domain.atlassian.net)
NUXT_CONFLUENCE_DOMAIN=
# Confluence user email
NUXT_CONFLUENCE_EMAIL=
# Confluence API Token
NUXT_CONFLUENCE_API_TOKEN=

